# LibLocker

Система централизованного контроля, учета времени использования и дистанционного управления компьютерами в локальной сети.

## 📋 Описание

LibLocker — это программный комплекс для управления компьютерами в компьютерных клубах, библиотеках, учебных классах и других местах, где требуется контроль времени использования ПК.

### Основные возможности

**Серверное приложение (Панель администратора):**
- 🖥️ Управление клиентами с уникальной идентификацией по HWID
- ⏰ Дистанционное создание сессий (быстрые кнопки +30 мин добавляет время, Безлимит, произвольное время)
- 📊 Детальная статистика и отчеты (ежедневные, помесячные)
- 💰 Гибкая система тарификации с настройкой стоимости и округления
- 📄 Экспорт отчетов в PDF
- 🌐 **Веб-интерфейс для управления со смартфонов и планшетов** ⭐ NEW
- 🔄 Мониторинг состояния клиентов в режиме реального времени

**Клиентское приложение (Агент):**
- ⏱️ **Виджет таймера** во время сессии (компактный, в левом верхнем углу, скрываемый)
- 🔒 Полноэкранная блокировка **после окончания** времени сессии
- 💰 Отображение оставшегося времени и стоимости
- 🛡️ Защита от несанкционированного завершения работы
- 🔌 Автоматическое переподключение при разрыве связи
- 📴 Автономный режим работы с синхронизацией
- 🚀 **Автозагрузка при старте Windows** ⭐ NEW
- 🎯 **Запуск в свернутом режиме (в трее)** ⭐ NEW
- 🔍 **Автоматическое обнаружение сервера в локальной сети** ⭐ NEW

## 🚀 Быстрый старт

### Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### Настройка

```bash
# Создайте конфигурационные файлы
python setup_config.py
```

Или вручную:
```bash
copy config.example.ini config.ini
copy config.client.example.ini config.client.ini
```

Отредактируйте файлы конфигурации под свои нужды.

### Запуск серверного приложения

```bash
python run_server.py
```

Откроется панель администратора с графическим интерфейсом.

### Запуск клиентского приложения

```bash
python run_client.py
```

По умолчанию клиент подключается к серверу по адресу `http://localhost:8765`.

Для подключения к удаленному серверу:
```bash
python run_client.py http://192.168.1.100:8765
```

**Запуск в свернутом режиме (в системном трее):**
```bash
python run_client.py --minimized
```

**Автоматическое обнаружение сервера:**
1. Запустите клиент
2. Щелкните правой кнопкой мыши по иконке в системном трее
3. Выберите "🔍 Поиск сервера..."
4. Нажмите "Найти серверы" - клиент автоматически найдет все серверы в локальной сети

**Автозапуск при загрузке системы:**
1. Запустите клиент
2. Щелкните правой кнопкой мыши по иконке в системном трее
3. Установите галочку "🚀 Автозапуск при загрузке системы"
4. Клиент будет автоматически запускаться в свернутом режиме при старте Windows

Подробнее: [Документация по обнаружению сервера](docs/SERVER_DISCOVERY.md)

### Веб-интерфейс для управления

После запуска сервера с включенным веб-интерфейсом:

1. Откройте браузер на любом устройстве в локальной сети
2. Перейдите по адресу `http://<IP-сервера>:8080`
3. Войдите с паролем администратора
4. Управляйте клиентами прямо из браузера!

**Возможности веб-интерфейса:**
- 👥 Просмотр всех подключенных клиентов в реальном времени
- ▶️ Запуск сессий с указанием времени или безлимит
- ⏹️ Остановка активных сессий
- 🔓 Разблокировка клиентских компьютеров
- 📱 Полная поддержка мобильных устройств

Подробнее: [Документация веб-интерфейса](docs/WEB_INTERFACE.md)

## 📁 Структура проекта

```
LibLocker/
├── src/
│   ├── shared/          # Общие модули для сервера и клиента
│   │   ├── models.py    # Модели данных
│   │   ├── protocol.py  # Протокол связи
│   │   ├── database.py  # Работа с БД
│   │   └── utils.py     # Утилиты
│   ├── server/          # Серверное приложение
│   │   ├── server.py    # WebSocket сервер
│   │   └── gui.py       # GUI панели администратора
│   ├── client/          # Клиентское приложение
│   │   ├── client.py    # WebSocket клиент
│   │   └── gui.py       # GUI агента и окно блокировки
│   └── web/             # Веб-интерфейс (в разработке)
├── data/                # База данных
├── tests/               # Тесты
├── docs/                # Документация
├── requirements.txt     # Python зависимости
├── run_server.py        # Запуск сервера
├── run_client.py        # Запуск клиента
└── Plan.txt             # Техническое задание
```

## ⚙️ Настройка

### Быстрая настройка

Запустите скрипт автоматической настройки:
```bash
python setup_config.py
```

### Серверное приложение

Редактируйте `config.ini`:

- **Тарификация:**
  - `free_mode` - Бесплатный режим (true/false)
  - `hourly_rate` - Стоимость руб./час
  - `rounding_minutes` - Округление времени (минуты)

- **Сетевые настройки:**
  - `host` - Адрес для прослушивания (0.0.0.0 = все интерфейсы)
  - `port` - Порт WebSocket сервера (по умолчанию 8765)
  - `web_port` - Порт веб-интерфейса (по умолчанию 8080)

- **База данных:**
  - `path` - Путь к файлу БД SQLite

- **Логирование:**
  - `level` - Уровень логирования (DEBUG, INFO, WARNING, ERROR)
  - `file` - Путь к файлу логов

### Клиентское приложение

Редактируйте `config.client.ini`:

- **Подключение:**
  - `url` - Адрес сервера (http://localhost:8765)
  - `connection_timeout` - Таймаут подключения (секунды)
  - `reconnect_interval` - Интервал переподключения

- **Виджет таймера:**
  - `position_x`, `position_y` - Позиция на экране
  - `width`, `height` - Размер виджета
  - `opacity` - Прозрачность (0-255)

- **Уведомления:**
  - `warning_minutes` - Предупреждать за N минут (по умолчанию 5)
  - `sound_enabled` - Звуковые уведомления (true/false)
  - `popup_enabled` - Всплывающие окна (true/false)

- **Безопасность:**
  - `admin_password_hash` - Хеш пароля для локальной разблокировки
  - `auto_unlock_timeout` - Автоматическая разблокировка при потере связи (минуты)

Подробнее: [Документация по конфигурации](docs/CONFIG_SYSTEM_UPDATE.md)

## 🔐 Безопасность

- **Идентификация:** Каждый клиент идентифицируется по уникальному HWID (на основе серийных номеров оборудования)
- **Защита паролей:** Пароли администратора хранятся в хешированном виде (bcrypt)
- **Шифрование:** Трафик между клиентом и сервером защищен (планируется TLS)
- **Защита процесса:** Клиентское приложение защищено от закрытия через Task Manager

## 🛠️ Разработка

### Текущий статус

✅ Реализовано:
- Базовая архитектура клиент-сервер
- WebSocket соединение с автопереподключением
- GUI серверного приложения (PyQt6)
- GUI клиентского приложения с окном блокировки
- **Компактный виджет-таймер во время сессии** ⭐
- **Полноэкранная блокировка после окончания сессии** ⭐
- **Автоматическое обнаружение сервера в локальной сети** ⭐
- **Сборка в единый исполняемый файл через PyInstaller** ⭐
- **Веб-интерфейс для управления через браузер** ⭐ NEW
- База данных для хранения клиентов и сессий
- Система идентификации клиентов по HWID
- Управление сессиями (создание, остановка)
- Таймер обратного отсчета с отображением стоимости
- Базовая система тарификации
- Поддержка безлимитных сессий

🚧 В разработке:
- Экспорт отчетов в PDF
- Автономный режим клиента с синхронизацией
- Установка клиента как Windows Service
- Расширенная защита процесса
- TLS шифрование

### Технологический стек

- **Язык:** Python 3.12+
- **GUI:** PyQt6
- **Сетевое взаимодействие:** Socket.IO (WebSocket)
- **База данных:** SQLite (SQLAlchemy ORM)
- **Веб-фреймворк:** Flask (планируется)
- **PDF генерация:** ReportLab / WeasyPrint

## 📝 Использование

### Сборка исполняемых файлов (PyInstaller)

Для использования приложения в виде единых исполняемых файлов:

**Windows:**
```bash
# Установите PyInstaller если еще не установлен
pip install pyinstaller

# Запустите сборку
build.bat
```

**Linux/macOS:**
```bash
# Установите PyInstaller если еще не установлен
pip install pyinstaller

# Запустите сборку
chmod +x build.sh
./build.sh
```

Готовые файлы будут в папке `dist/`:
- `LibLockerServer.exe` (или `LibLockerServer` на Linux/macOS)
- `LibLockerClient.exe` (или `LibLockerClient` на Linux/macOS)

Эти файлы можно копировать на любые компьютеры без установки Python.

Подробнее: [Документация по обнаружению сервера](docs/SERVER_DISCOVERY.md)

### Создание сессии

1. Откройте панель администратора
2. Выберите клиента в таблице
3. Нажмите "Начать сессию"
4. Выберите длительность:
   - Быстрые кнопки: +30 минут, Безлимит
   - Или укажите произвольное время (часы и минуты)
5. Нажмите "Создать"

**На компьютере клиента:**
- Появится **компактный виджет-таймер** в левом верхнем углу экрана
- Виджет показывает оставшееся время и стоимость
- Пользователь может свободно работать во время сессии
- Виджет можно свернуть нажатием на кнопку "×"
- Когда время истечет, автоматически появится **полноэкранная блокировка**

Подробнее: [Руководство по виджету таймера](docs/TIMER_WIDGET_GUIDE.md)

### Остановка сессии

1. Выберите клиента с активной сессией
2. Нажмите "Остановить сессию"

Окно блокировки на клиенте закроется автоматически.

### Просмотр статистики

1. Перейдите на вкладку "Статистика"
2. Просмотрите историю всех сессий
3. При необходимости экспортируйте отчет в PDF

## 🤝 Вклад в проект

Проект находится в активной разработке. Приветствуются:
- Сообщения об ошибках
- Предложения по улучшению
- Pull requests

## 📄 Лицензия

См. файл [LICENSE](LICENSE)

## 👨‍💻 Автор

Проект разработан согласно техническому заданию из файла `Plan.txt`

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории проекта.
