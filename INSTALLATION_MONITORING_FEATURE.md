# Функция мониторинга установки программ

## Описание

Новая функция позволяет администратору дистанционно включать и выключать мониторинг установки программ на клиентских компьютерах. При обнаружении попытки установки программы или скачивания установочного файла, компьютер немедленно блокируется красным экраном тревоги с громкой сиреной.

## Возможности

### 1. Детекция установки программ

Система отслеживает:
- **Запуск установочных процессов**: setup.exe, install.exe, msiexec.exe и другие
- **Скачивание установочных файлов**: .exe, .msi, .bat, .cmd, .ps1, .vbs, .jar файлы в папке "Загрузки"
- **Мониторинг в реальном времени**: проверка каждые 2 секунды

### 2. Красный экран тревоги

При обнаружении установки:
- 🔴 **Ярко-красный полноэкранный режим** с эффектом мигания
- ⚠️ **Крупное предупреждающее сообщение**
- 🔊 **Громкая сирена** с настраиваемой громкостью
- 🚫 **Блокировка клавиатуры и мыши**
- ⏹️ **Немедленное прерывание активной сессии**

### 3. Управление с сервера

Администратор может:
- ✅ **Включать/выключать** мониторинг для каждого клиента
- 🎛️ **Управлять через GUI** сервера
- 📊 **Отслеживать статус** мониторинга

### 4. Локальное управление

Пользователь (с правами) может:
- 🖱️ **Правый клик** на виджете таймера
- ☑️ **Включить/выключить** мониторинг через контекстное меню
- 👁️ **Видеть текущий статус** (галочка в меню)

## Настройка

### Конфигурация клиента

В файле `config.client.ini`:

```ini
[installation_monitor]
# Мониторинг установки программ (управляется с сервера)
enabled = false
# Громкость сирены при обнаружении установки (0-100)
alert_volume = 80
```

### Параметры

- **enabled**: Включен ли мониторинг (по умолчанию: false)
- **alert_volume**: Громкость сирены от 0 до 100 (по умолчанию: 80)

## Использование

### Для администратора (серверная панель)

1. Запустите серверное приложение
2. Перейдите на вкладку "Клиенты"
3. Выберите нужный клиент
4. Нажмите кнопку **"🔍 Мониторинг установки"**
5. Кнопка станет активной - мониторинг включен
6. Повторное нажатие отключает мониторинг

### Для пользователя (клиентское приложение)

1. Во время сессии виден виджет таймера
2. **Правый клик** на виджете таймера
3. Появится контекстное меню
4. Пункт **"Включить мониторинг установки программ"**
5. Галочка показывает текущий статус
6. Клик переключает состояние

## Технические детали

### Обнаружение установки

#### Мониторинг процессов
```python
# Отслеживаемые процессы
INSTALLER_PROCESSES = {
    'msiexec.exe', 'setup.exe', 'install.exe', 'installer.exe',
    'unins000.exe', 'uninst.exe', 'uninstall.exe',
    'inno_updater.exe', 'installshield.exe', 'wise.exe',
    'nsis.exe', 'isetup.exe'
}
```

#### Мониторинг файлов
```python
# Отслеживаемые расширения (Windows)
INSTALLER_EXTENSIONS = {'.exe', '.msi', '.bat', '.cmd', '.ps1', '.vbs', '.jar'}

# Отслеживаемые папки
DOWNLOAD_FOLDERS = [
    Path.home() / "Downloads",
    Path.home() / "Загрузки",
    Path.home() / "Desktop",
    Path.home() / "Рабочий стол"
]
```

### Протокол связи

Новый тип сообщения:
```python
MessageType.INSTALLATION_MONITOR_TOGGLE = "installation_monitor_toggle"

@dataclass
class InstallationMonitorToggleMessage:
    enabled: bool  # True = включить, False = выключить
```

### Архитектура

```
Server (GUI) → toggle_installation_monitor()
    ↓
WebSocket Message → Client
    ↓
ClientThread → on_installation_monitor_toggle()
    ↓
MainClientWindow → InstallationMonitor.start()/stop()
    ↓
Background Thread → Monitoring Loop
    ↓ (detection)
RedAlertLockScreen (Fullscreen + Siren)
```

## Звуковые эффекты

### Воспроизведение звука

Система поддерживает несколько способов воспроизведения:

1. **PyQt6.QtMultimedia** (предпочтительно):
   - Полный контроль громкости
   - Зацикливание звука
   - Высокое качество

2. **winsound** (резервный, Windows):
   - Системные звуки
   - Работает без дополнительных библиотек
   - Повторяющиеся сигналы

3. **Без звука** (для других ОС):
   - Только визуальная тревога
   - Мигающий красный экран

### Встроенный звук

Звуковой файл `siren.wav`:
- Формат: WAV, 16-bit, Mono, 44100 Hz
- Длительность: 2 секунды (зацикливается)
- Эффект: Сирена с частотой 800-1200 Hz
- Встраивается в программу через base64

## Требования

### Зависимости

```
psutil==5.9.8           # Мониторинг процессов
pycaw==20240210         # Управление громкостью (Windows)
PyQt6-Multimedia==6.6.1 # Воспроизведение аудио (опционально)
```

### Платформы

- ✅ **Windows**: Полная поддержка (процессы, файлы, звук, громкость)
- ⚠️ **macOS**: Частичная поддержка (без управления громкостью)
- ⚠️ **Linux**: Частичная поддержка (без управления громкостью)

## Безопасность

### Меры защиты

1. **Блокировка обхода**:
   - Полноэкранный режим без рамки
   - Блокировка всех клавиш (Alt+F4, Esc, Win)
   - Блокировка кликов мыши
   - Всегда поверх всех окон

2. **Детектирование**:
   - Проверка процессов в реальном времени
   - Отслеживание новых файлов
   - Запуск в фоновом потоке

3. **Проверки безопасности**:
   - ✅ CodeQL сканирование: 0 уязвимостей
   - ✅ Code Review: все замечания устранены

## Тестирование

### Автоматический тест

Запустите тест:
```bash
python test_installation_monitor.py
```

Тест проверяет:
- ✅ Запуск и остановку мониторинга
- ✅ Обнаружение новых установочных файлов
- ✅ Вызов callback функции
- ✅ Очистку ресурсов

### Ручное тестирование

1. **Тест контекстного меню**:
   - Правый клик на виджете → меню появляется
   - Галочка отражает статус
   - Переключение работает

2. **Тест детекции файла**:
   - Включите мониторинг
   - Скачайте .exe файл
   - Появится красный экран

3. **Тест детекции процесса**:
   - Включите мониторинг
   - Запустите setup.exe
   - Появится красный экран

4. **Тест сервера**:
   - Выберите клиента
   - Нажмите кнопку мониторинга
   - Статус изменится на клиенте

## Примеры использования

### Сценарий 1: Компьютерный класс

Администратор включает мониторинг на всех компьютерах класса. Если студент попытается установить игру или другую программу, компьютер немедленно заблокируется с громким предупреждением, привлекая внимание преподавателя.

### Сценарий 2: Библиотека

В библиотеке включен мониторинг на общедоступных компьютерах. При попытке скачать и установить вредоносное ПО, система мгновенно блокирует компьютер, предотвращая заражение.

### Сценарий 3: Офис

На рабочих местах включен мониторинг для предотвращения установки несанкционированного ПО, обеспечивая безопасность корпоративной сети.

## Ограничения

1. **Производительность**: Проверка каждые 2 секунды минимально влияет на производительность
2. **Ложные срабатывания**: Любой .exe файл в Downloads вызовет тревогу
3. **Обход**: Опытные пользователи могут установить из других папок (требуется дополнительная настройка)
4. **Платформы**: Полная функциональность только на Windows

## Будущие улучшения

Возможные доработки:
- 🔄 Белый список программ (разрешенные установщики)
- 📝 Логирование всех попыток установки
- 📧 Уведомления администратора по email
- 🌐 Мониторинг установки через браузер
- 🔍 Расширенный поиск установщиков в других папках
- 📊 Статистика попыток установки

## Лицензия

Часть проекта LibLocker. См. LICENSE для деталей.
