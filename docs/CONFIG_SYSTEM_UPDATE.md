# Обновление - Система конфигурации и уведомлений

## Версия: 2026-01-14 (Вечерняя)

### 🎯 Новые возможности

#### 1. Система конфигурации

**Конфигурация клиента** (`config.client.ini`):
- Настройки подключения к серверу
- Настройки внешнего вида виджета (позиция, размер, прозрачность)
- Настройки уведомлений (звук, всплывающие окна, время предупреждения)
- Настройки безопасности (пароль, таймаут автоматической разблокировки)
- Настройки логирования
- Настройки автозапуска

**Конфигурация сервера** (`config.ini`):
- Улучшена поддержка через класс ServerConfig
- Автоматическое создание из example файла
- Типизированные методы получения значений

#### 2. Уведомления и предупреждения

**Предупреждение о скором окончании сессии:**
- Настраиваемое время предупреждения (по умолчанию за 5 минут)
- Звуковое уведомление (системный звук Windows)
- Всплывающее окно с сообщением
- Автоматическое разворачивание виджета если он был скрыт

**Визуальные индикаторы:**
- 🟢 Более 5 минут - белый цвет таймера
- 🟠 Последние 5 минут - оранжевый цвет
- 🔴 Последняя минута - красный цвет

#### 3. Улучшенный виджет таймера

**Настраиваемые параметры:**
```ini
[widget]
position_x = 20          # X координата виджета
position_y = 20          # Y координата виджета
width = 200              # Ширина виджета
height = 100             # Высота виджета
opacity = 240            # Прозрачность (0-255)
auto_hide_after = 0      # Автоскрытие через N секунд
```

**Новые функции:**
- Настраиваемая позиция на экране
- Настраиваемый размер
- Настраиваемая прозрачность фона
- Цветовая индикация оставшегося времени

#### 4. Улучшенное логирование

**Логирование клиента:**
- Запись в файл `logs/client.log`
- Настраиваемый уровень логирования (DEBUG, INFO, WARNING, ERROR)
- Логи с временными метками и уровнями
- Одновременный вывод в файл и консоль

**Логирование сервера:**
- Запись в файл `logs/server.log`
- Аналогичные возможности

### 📋 Конфигурационные файлы

#### config.client.example.ini
Пример конфигурации для клиента со всеми доступными опциями:
```ini
[server]
url = http://localhost:8765
connection_timeout = 10
reconnect_interval = 5

[widget]
position_x = 20
position_y = 20
width = 200
height = 100
opacity = 240
auto_hide_after = 0

[notifications]
warning_minutes = 5
sound_enabled = true
popup_enabled = true

[security]
admin_password_hash =
auto_unlock_timeout = 10

[logging]
level = INFO
file = logs/client.log

[autostart]
enabled = false
auto_connect = true
```

### 🔧 Технические детали

#### Класс ClientConfig
```python
from src.shared.config import ClientConfig

config = ClientConfig()  # Загружает config.client.ini

# Получение значений
server_url = config.server_url
widget_pos = config.widget_position  # returns (x, y)
warning_time = config.warning_minutes
sound_on = config.sound_enabled
```

#### Класс ServerConfig
```python
from src.shared.config import ServerConfig

config = ServerConfig()  # Загружает config.ini

# Получение значений
host = config.host
port = config.port
free_mode = config.free_mode
hourly_rate = config.hourly_rate
```

### 🎨 Пример использования

#### Установка предупреждения за 3 минуты
```ini
[notifications]
warning_minutes = 3
```

#### Перемещение виджета в правый верхний угол
```ini
[widget]
position_x = 1720  # Для экрана 1920x1080
position_y = 20
```

#### Отключение звуковых уведомлений
```ini
[notifications]
sound_enabled = false
popup_enabled = true  # Оставить только визуальные
```

#### Увеличение прозрачности виджета
```ini
[widget]
opacity = 180  # Более прозрачный (0-255)
```

### 🚀 Инструкция по установке

1. **Скопируйте example файлы:**
```powershell
copy config.client.example.ini config.client.ini
copy config.example.ini config.ini
```

2. **Отредактируйте конфигурацию** под свои нужды

3. **Запустите приложения:**
```powershell
python run_server.py
python run_client.py
```

Приложения автоматически загрузят настройки из конфигурационных файлов.

### 📊 Преимущества

✅ **Гибкость** - настройка без изменения кода
✅ **Удобство** - все параметры в одном месте
✅ **Контроль** - пользователь видит когда заканчивается время
✅ **Безопасность** - настраиваемые параметры безопасности
✅ **Диагностика** - подробное логирование в файлы

### 🐛 Известные ограничения

- Звуковые уведомления работают только на Windows (используется winsound)
- Настройки применяются при перезапуске приложения
- Изменение позиции виджета требует редактирования config файла

### 🔜 Планы на будущее

- GUI для настройки параметров виджета
- Больше вариантов звуковых уведомлений
- Автоматическое обновление конфигурации без перезапуска
- Профили настроек для разных сценариев использования
- Drag & drop перемещение виджета мышью

### 📝 Миграция

При обновлении с предыдущей версии:
1. Скопируйте example файлы конфигурации
2. Настройте параметры по вашему усмотрению
3. Запустите клиент - он автоматически использует новую систему

Старый код совместим - если конфигурация отсутствует, используются значения по умолчанию.

