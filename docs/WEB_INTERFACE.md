# Веб-интерфейс LibLocker

## Описание

Веб-интерфейс позволяет управлять клиентами LibLocker через браузер с любого устройства в локальной сети (компьютер, смартфон, планшет).

## Возможности

- 🔐 **Защита паролем** - для входа требуется пароль администратора сервера
- 👥 **Просмотр клиентов** - список всех подключенных клиентов с их статусом
- ▶️ **Запуск сессий** - удаленный запуск сессий с указанием времени или безлимит
- ⏹️ **Остановка сессий** - остановка активных сессий
- 🔓 **Разблокировка ПК** - снятие блокировки с клиентских компьютеров
- 🔄 **Автообновление** - список клиентов обновляется каждые 3 секунды

## Настройка

### 1. Включение веб-сервера

Откройте панель администратора и перейдите на вкладку **Настройки**:

1. Найдите раздел "Сетевые настройки"
2. Установите галочку **"Включить веб-сервер для управления через браузер"**
3. При необходимости измените веб-порт (по умолчанию 8080)
4. Нажмите **"Сохранить настройки"**
5. **Перезапустите сервер** для применения изменений

### 2. Установка пароля администратора

Если пароль еще не установлен:

1. На вкладке **Настройки** найдите раздел "Безопасность"
2. Введите новый пароль (минимум 8 символов)
3. Подтвердите пароль
4. Нажмите **"Установить пароль"**

### 3. Ручная настройка через config.ini

Вы также можете включить веб-сервер, отредактировав файл `config.ini`:

```ini
[server]
host = 0.0.0.0
port = 8765
web_port = 8080
web_server_enabled = true
```

## Использование

### Доступ к веб-интерфейсу

После включения и запуска сервера, веб-интерфейс будет доступен по адресу:

```
http://<IP-адрес-сервера>:8080
```

Например:
- `http://localhost:8080` - с того же компьютера
- `http://192.168.1.100:8080` - с других устройств в сети

### Вход в систему

1. Откройте веб-интерфейс в браузере
2. Введите пароль администратора
3. Нажмите **"Войти"**

### Управление клиентами

После входа вы увидите таблицу с клиентами:

| Колонка | Описание |
|---------|----------|
| **Имя** | Имя клиентского компьютера |
| **IP адрес** | IP-адрес клиента в сети |
| **Статус** | Онлайн / Оффлайн / В сессии |
| **Сессия** | Оставшееся время или "Безлимит" |
| **Действия** | Кнопки управления |

#### Запуск сессии

1. Нажмите кнопку **"Запустить"** для выбранного клиента
2. В открывшемся окне укажите:
   - Длительность в минутах (например, 30)
   - Или установите галочку **"Безлимитная сессия"**
3. Нажмите **"Запустить"**

Сессия начнется на клиентском компьютере.

#### Остановка сессии

1. Нажмите кнопку **"Остановить"** для клиента с активной сессией
2. Подтвердите действие

Сессия будет завершена, и на клиенте появится экран блокировки.

#### Разблокировка клиента

1. Нажмите кнопку **"Разблокировать"** для выбранного клиента
2. Подтвердите действие

Экран блокировки на клиенте будет снят.

## Безопасность

- ✅ Все действия требуют авторизации
- ✅ Пароли хранятся в хешированном виде (bcrypt)
- ✅ Сеансы защищены токенами
- ⚠️ Веб-сервер доступен только в локальной сети
- ⚠️ Используйте надежный пароль администратора

## Мобильные устройства

Веб-интерфейс адаптирован для использования на смартфонах и планшетах:

1. Убедитесь, что мобильное устройство подключено к той же сети, что и сервер
2. Откройте браузер на устройстве
3. Перейдите по адресу `http://<IP-сервера>:8080`
4. Войдите с паролем администратора

## Технические детали

- **Фреймворк**: aiohttp + aiohttp-jinja2
- **Асинхронность**: Полностью асинхронный веб-сервер
- **Шаблоны**: Jinja2
- **API**: RESTful JSON API
- **Автообновление**: JavaScript fetch с интервалом 3 секунды

## API Endpoints

Для разработчиков доступны следующие API endpoints:

### POST /api/login
Вход в систему
```json
Request: {"password": "admin_password"}
Response: {"success": true, "token": "authenticated"}
```

### GET /api/clients
Получить список клиентов (требуется авторизация)
```
Headers: {"Authorization": "Bearer authenticated"}
Response: {"success": true, "clients": [...]}
```

### POST /api/start_session
Запустить сессию (требуется авторизация)
```json
Request: {
  "client_id": 1,
  "duration_minutes": 30,
  "is_unlimited": false
}
Response: {"success": true, "message": "..."}
```

### POST /api/stop_session
Остановить сессию (требуется авторизация)
```json
Request: {"client_id": 1}
Response: {"success": true, "message": "..."}
```

### POST /api/unlock_client
Разблокировать клиента (требуется авторизация)
```json
Request: {"client_id": 1}
Response: {"success": true, "message": "..."}
```

### POST /api/logout
Выход из системы (требуется авторизация)
```
Response: {"success": true}
```

## Устранение неполадок

### Веб-сервер не запускается

1. Убедитесь, что в настройках включен веб-сервер (`web_server_enabled = true`)
2. Проверьте, что порт не занят другим приложением
3. Проверьте логи сервера на наличие ошибок
4. Убедитесь, что установлена зависимость `aiohttp-jinja2`

### Не могу войти

1. Убедитесь, что пароль администратора установлен на сервере
2. Проверьте правильность ввода пароля
3. Если забыли пароль, установите новый через панель администратора

### Не вижу клиентов

1. Убедитесь, что клиенты подключены к серверу
2. Проверьте статус клиентов в основной панели администратора
3. Подождите несколько секунд - список автоматически обновляется

### Не могу подключиться с другого устройства

1. Убедитесь, что устройства находятся в одной сети
2. Проверьте, что брандмауэр не блокирует порт
3. Используйте правильный IP-адрес сервера (не localhost)
4. Убедитесь, что сервер запущен с `host = 0.0.0.0` (слушает все интерфейсы)

## Примеры использования

### Сценарий 1: Управление из офиса

Администратор сидит за своим компьютером и через веб-интерфейс на ноутбуке управляет всеми клиентами в библиотеке.

### Сценарий 2: Удаленное управление

Администратор находится в другой комнате с планшетом и через веб-интерфейс запускает/останавливает сессии, не подходя к серверу.

### Сценарий 3: Мобильный контроль

Администратор использует смартфон для быстрой проверки статуса клиентов и разблокировки ПК при необходимости.

## Дальнейшее развитие

Планируемые улучшения:

- [ ] Поддержка HTTPS (TLS/SSL шифрование)
- [ ] Расширенные права доступа (несколько пользователей)
- [ ] История действий в веб-интерфейсе
- [ ] Графики и статистика использования
- [ ] Push-уведомления о событиях
- [ ] Темная тема интерфейса
- [ ] Экспорт отчетов из веб-интерфейса

## Обратная связь

При возникновении проблем или предложений по улучшению веб-интерфейса, создайте Issue в репозитории проекта.
