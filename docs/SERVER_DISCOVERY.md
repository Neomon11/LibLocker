# Автоматическое обнаружение сервера в локальной сети

## Описание

LibLocker теперь поддерживает автоматическое обнаружение сервера в локальной сети. Это особенно полезно при использовании приложения в виде скомпилированных исполняемых файлов (через PyInstaller), когда нужно быстро настроить клиенты без ручного ввода IP-адресов.

## Возможности

### 1. Автоматическое обнаружение через UDP broadcast
- Сервер автоматически объявляет о своем присутствии в локальной сети
- Клиент может найти все доступные серверы одним нажатием кнопки
- Использует UDP broadcast на порту 8766
- Не требует дополнительной настройки

### 2. Ручной ввод адреса сервера
- Если автоматическое обнаружение не работает (например, из-за настроек сетевого экрана)
- Можно вручную ввести IP-адрес и порт сервера
- Формат: `http://192.168.1.100:8765`

### 3. Сохранение конфигурации
- Выбранный сервер сохраняется в файл `config.client.ini`
- При следующем запуске клиент автоматически подключится к сохраненному серверу

## Использование

### Для сервера

Сервер автоматически начинает объявлять о себе при запуске. Никаких дополнительных действий не требуется.

```bash
# Запуск сервера
python run_server.py

# Или запуск скомпилированного исполняемого файла
LibLockerServer.exe
```

При запуске в логах вы увидите:
```
Server announcer started on port 8766
Listening for discovery requests on port 8766
```

### Для клиента

#### Вариант 1: Автоматический поиск через GUI

1. Запустите клиент
2. Щелкните правой кнопкой мыши по иконке в системном трее
3. Выберите "🔍 Поиск сервера..."
4. В открывшемся окне нажмите "🔍 Найти серверы"
5. Подождите несколько секунд - в списке появятся найденные серверы
6. Выберите сервер из списка или введите адрес вручную
7. Нажмите "Подключиться"
8. Перезапустите клиент для подключения к новому серверу

#### Вариант 2: Ручной ввод в конфигурационном файле

Отредактируйте `config.client.ini`:

```ini
[server]
url = http://192.168.1.100:8765
```

#### Вариант 3: Передача URL через командную строку

```bash
python run_client.py http://192.168.1.100:8765

# Или для скомпилированного файла
LibLockerClient.exe http://192.168.1.100:8765
```

## Сборка исполняемых файлов

### Windows

```bash
# Установка зависимостей (если еще не установлены)
pip install -r requirements.txt
pip install pyinstaller

# Запуск сборки
build.bat
```

Исполняемые файлы будут созданы в папке `dist/`:
- `LibLockerServer.exe` - серверное приложение
- `LibLockerClient.exe` - клиентское приложение

### Linux / macOS

```bash
# Установка зависимостей (если еще не установлены)
pip install -r requirements.txt
pip install pyinstaller

# Запуск сборки
chmod +x build.sh
./build.sh
```

## Технические детали

### Протокол обнаружения

Клиент отправляет UDP broadcast запрос на порт 8766:
```json
{
    "magic": "LIBLOCKER_DISCOVERY",
    "type": "discovery_request",
    "timestamp": 1234567890.123
}
```

Сервер отвечает:
```json
{
    "magic": "LIBLOCKER_DISCOVERY",
    "type": "discovery_response",
    "port": 8765,
    "name": "LibLocker Server",
    "timestamp": 1234567890.456
}
```

### Порты

- **8765** - основной порт WebSocket сервера (настраивается в `config.ini`)
- **8766** - порт для UDP broadcast обнаружения (фиксированный)

### Безопасность

- Обнаружение работает только в локальной сети (broadcast)
- Не передается никакая конфиденциальная информация
- Рекомендуется использовать в доверенных сетях

### Устранение неполадок

#### Серверы не обнаруживаются

1. **Проверьте сетевой экран (firewall)**
   - Разрешите входящие UDP соединения на порт 8766 для сервера
   - Разрешите исходящие UDP broadcast для клиента

2. **Проверьте, что клиент и сервер в одной подсети**
   - UDP broadcast не проходит через маршрутизаторы
   - Клиент и сервер должны быть в одной локальной сети (например, 192.168.1.x)

3. **Используйте ручной ввод адреса**
   - Если автоматическое обнаружение не работает, введите IP-адрес вручную

#### Проверка логов

**Сервер** (`logs/server.log`):
```
Server announcer started on port 8766
Listening for discovery requests on port 8766
Received discovery request from 192.168.1.50
Sent discovery response to 192.168.1.50
```

**Клиент** (`logs/client.log`):
```
Starting server discovery from UI
Sending discovery broadcast on port 8766
Found server: ServerInfo(ip=192.168.1.100, port=8765, name=LibLocker Server)
Discovery complete. Found 1 server(s)
```

## Примеры использования

### Сценарий 1: Компьютерный клуб

1. Установите сервер на администраторский компьютер
2. Скомпилируйте клиент в один исполняемый файл
3. Скопируйте `LibLockerClient.exe` на все клиентские компьютеры
4. При первом запуске на каждом клиенте используйте автоматический поиск
5. Клиенты автоматически найдут и подключатся к серверу

### Сценарий 2: Библиотека

1. Запустите сервер на одном из компьютеров
2. Узнайте IP-адрес этого компьютера (например, 192.168.1.100)
3. На всех клиентах создайте `config.client.ini` с правильным адресом
4. Распространите клиент вместе с конфигурацией

### Сценарий 3: Домашняя сеть

1. Запустите сервер на основном компьютере
2. На других компьютерах запустите клиент
3. Используйте автоматический поиск для подключения
4. Конфигурация сохранится и будет использоваться при следующих запусках

## Дополнительные возможности

### Fallback механизм

Если UDP broadcast не работает, модуль обнаружения имеет fallback механизм для сканирования подсети методом прямого подключения. Это более медленный метод, но работает в любых условиях.

### Множественные серверы

Если в сети несколько серверов LibLocker, клиент покажет их все в списке. Вы можете выбрать любой из них.

### Обновление адреса сервера

Вы можете в любой момент изменить сервер через меню в системном трее:
1. Правый клик по иконке клиента в трее
2. "🔍 Поиск сервера..."
3. Выберите новый сервер
4. Перезапустите клиент

## FAQ

**Q: Нужно ли открывать порты в firewall?**  
A: Для сервера - да, UDP 8766 и TCP 8765. Для клиента обычно не требуется.

**Q: Работает ли обнаружение через интернет?**  
A: Нет, только в локальной сети. Для удаленного доступа используйте VPN.

**Q: Можно ли изменить порт обнаружения?**  
A: Порт 8766 жестко закодирован в модуле `discovery.py`. При необходимости можно изменить константу `DISCOVERY_PORT`.

**Q: Что делать, если сервер не находится?**  
A: Используйте ручной ввод IP-адреса. Убедитесь, что сервер запущен и доступен в сети.

**Q: Безопасно ли использовать автоматическое обнаружение?**  
A: В доверенных локальных сетях - да. В публичных сетях рекомендуется использовать ручной ввод адреса.
